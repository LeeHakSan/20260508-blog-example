{{> layout/header}}

<div class="flex-grow-1 d-flex align-items-center justify-content-center">
    <div class="text-center px-3 py-5">
        <div class="error-code mb-2">401</div>
        <h4 class="fw-bold text-dark mb-2">로그인이 필요합니다</h4>
        <p class="text-muted mb-4">{{msg}}</p>
        <div class="d-flex gap-2 justify-content-center">
            <a href="/login-form" class="btn btn-primary">
                <i class="fa-solid fa-right-to-bracket me-1"></i>로그인
            </a>
            <a href="/" class="btn btn-outline-secondary">
                <i class="fa-solid fa-house me-1"></i>메인 페이지
            </a>
        </div>
    </div>
</div>

{{> layout/footer}}
